# V1 目标验收记录

## 结论

Python 多模型施工组织设计生成台 V1 的软件实现与本地交付链路已经完成。它已经脱离 Hermes、飞书和 MCP，使用 React 工作台、FastAPI 受控工作流、SQLite 状态持久化和国内多模型路由。

真实项目的文字质量、千问图纸识别效果和智谱争议复核效果，仍需在配置对应 API Key 后由首个真实项目验证。这属于计划中明确保留的业务测试，不等于用模型记忆代替证据。

## 目标覆盖

| 目标 | 状态 | 实现证据 |
|---|---|---|
| 删除 Hermes、飞书、MCP 核心依赖 | 已完成 | 旧配置、MCP 服务、注册脚本和测试已删除 |
| 运行数据统一到 LOCALAPPDATA | 已完成 | `config.py` 固定使用 `%LOCALAPPDATA%\PassBidWriter` |
| 整个文件夹导入并保留相对目录 | 已完成 | 工作台目录选择器与 multipart 导入 API |
| PDF、Office、图片、压缩包、DWG/DXF 输入 | 已完成 | 本地扫描、解压、分类和角色确认 |
| DWG 不直接解析，缺 PDF 必须阻断 | 已完成 | 图纸登记表和“图纸待转换”高风险复核项 |
| 图纸 PDF 高分辨率和分块识别 | 已完成 | 300 DPI 输出、整页加四象限分块、最多 8 张一批 |
| 图纸中心人工确认 | 已完成 | 图号、图名、图注、裁剪、章节、局部/横向整页、确认时间 |
| DeepSeek / 千问 / 智谱分工 | 已完成 | 文字、批量视觉、复杂视觉和争议复核独立路由 |
| 项目事实与编制依据分离 | 已完成 | 项目事实、规范条款、案例资产和 BasisLink 数据 |
| 任务书确认闸门 | 已完成 | 未确认文件角色、视觉分析和任务书时禁止写正文 |
| 严格逐章生成、重写和接受 | 已完成 | 第一未确认章节状态机，失败章节可单独重试 |
| 状态重启不丢失 | 已完成 | SQLite 工作流任务、章节、图纸和复核项持久化 |
| 人工复核项可编辑和补录事实 | 已完成 | 复核编辑 API 与工作台表单 |
| DOCX / PDF / 六份报告 | 已完成 | A4 Word、目录、页眉页脚、页码、原生表格、图表和报告 |
| Word COM 优先，LibreOffice 回退 | 已完成 | 两级 PDF 导出逻辑 |
| 独立 Python 3.12 环境 | 已完成 | `%LOCALAPPDATA%\PassBidWriter\venv` |
| 日常启动不下载依赖 | 已完成 | `start.ps1` 只做依赖探测和本地启动 |
| 发布包不要求 Node | 已完成 | ZIP 内含 `workbench/dist` |
| 旧版升级路径 | 已完成 | `update.ps1`、发布 ZIP 和 README 升级说明 |

## 已执行验证

- Python 自动化测试全部通过。
- 离线端到端流程生成 DOCX 和六份控制报告。
- Microsoft Word COM 成功导出 A4 PDF。
- PDF 全页渲染检查覆盖封面、目录、表格、图形和响应矩阵。
- 浏览器黑盒检查覆盖导航、图纸确认、横向整页插图和人工事实补录。
- 发布 ZIP 解压后使用独立运行环境启动，健康检查和首页均返回成功。

## 尚待真实项目验证

- 千问 API 的真实扫描件、复杂预算表和密集设计图识别质量。
- DeepSeek 在完整项目证据包上的逐章文字质量和成本。
- 智谱对低置信图纸数字及模型差异的二次复核效果。
- 公司正式模板、签章位置和最终投标版式的业务调整。
